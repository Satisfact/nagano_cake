<div class="col-xs-3">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>ジャンル検索</th>
        </tr>
      </thead>
      <tbody>
        <% genres.each do |genre| %>
          <tr>
            <td>
              <!--管理者側のgenreに行ってしまう-->
              <%= link_to genre.name, admins_genres_path(genre.id) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>